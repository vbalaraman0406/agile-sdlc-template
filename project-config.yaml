# =============================================================================
# Project Configuration — Auto-generated by project-init.py
# =============================================================================
# This file does not yet contain project-specific configuration.
# Run `python3 project-init.py` to generate a new project with this template.
# =============================================================================
#
# After running the init script, this file will contain:
#   - project: name, title, description, type
#   - stack: id, name
#   - agile: total_phases, phases (with id, type, name, status)
#   - quality_gates: list of all enabled gates
#
# The AI agent MUST read this file before starting any work.
# =============================================================================
